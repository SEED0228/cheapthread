<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="container">
            <%= link_to "", class:"navbar-brand", style: "color: yellow;" do%>
                <span>〇円ガチャ(仮)</span>
            <% end %>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav navbar-right ml-auto">
                    <li><%= link_to "ホーム", "/", class: "fas fa-home nav-link text-light"%></li>
                    <li><%= link_to "ユーザ一覧", end_users_path, class: "fas fa-users nav-link text-light"%></li>
                    <li><%= link_to "リスト一覧", "/", class: "fas fa-list nav-link text-light"%></li>
                    <% if end_user_signed_in? %>
                    <li><%= link_to "マイページ", end_user_path(current_end_user), class: "far fa-user nav-link text-light"%></li>
                    <li><%= link_to "ログアウト", destroy_end_user_session_path, method: :delete, class: "fas fa-sign-out-alt nav-link text-light"%></li>
                    <% else %>
                    <li><%= link_to "アカウント作成", new_end_user_registration_path, class: "fas fa-user-plus nav-link text-light"%></li>
                    <li><%= link_to "ログイン",  new_end_user_session_path, class: "fas fa-sign-in-alt nav-link text-light"%></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>
</header>